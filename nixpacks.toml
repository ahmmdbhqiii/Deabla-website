[providers]
providers = ['php']

[phases.setup]
nixPkgs = ['php84', 'composer']

[phases.build]
cmds = ['composer install --ignore-platform-reqs --no-dev --optimize-autoloader']

[start]
cmd = 'php artisan serve --host=0.0.0.0 --port=$PORT'